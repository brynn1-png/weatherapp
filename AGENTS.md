# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

Before modifying this project, also read `plan.md` and `rules.md`. WeatherAI is Android-only; do not add iOS deployment, configuration, testing, or release requirements.
